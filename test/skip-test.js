import {expect} from 'src/external/chai.js'

describe('Register Event Listeners', function() {
  it('should register events when adding events',  function() {
    this.skip()
  })
  it('should4 register events when adding events',  function() {
    // this.skip()
  })
  })
  
  
  