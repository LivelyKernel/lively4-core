#!/bin/bash

git status --porcelain | grep  "??" | sed 's/^?? /git add /' | bash
echo -n "SYNC " > COMMIT ; 
git status --porcelain | grep -v "??" | tr "\n" ";">> COMMIT; 
git status --porcelain | grep  "??" | sed 's/^?? /git add /gitgit pull --no-edit; 
git pull --no-edit; 
git pull --no-edit; 
git pull --no-edit; 
git pull --no-edit; 
git pull --no-edit; 
 pull --no-edit; 
git pull --no-edit; 

# git commit -F COMMIT .; 
# git push origin gh-pages
