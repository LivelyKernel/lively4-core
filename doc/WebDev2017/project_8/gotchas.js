function getDescriptionBroken (name, features) {
  return
    `This is the description of product ${name}.
    It's features are: ${features.join()}`
}


for (let item of [1, 2, 3]);
  console.info(item);


const math = 3 + 4 * 5 + 6 * 7 * 8
