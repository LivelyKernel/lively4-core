"enable aexpr";

import Morph from 'src/components/widgets/lively-morph.js';

export default class BlockchainTest extends Morph {
  async initialize() {
    this.windowTitle = "BlockchainTest";
  }
  
}