var a = 3;
console.log(a + 4)

let obj = {
  func(x) { return x + 2; }
}

obj.func(a)
const hash = Math.random();
aexpr(()=>a).onChange(val => lively.notify(val, hash));
a = 42;
a = 43;

class A { static foo() {}}
class B {
  static foo() {
    //super.foo()
  }
}

B.foo()

