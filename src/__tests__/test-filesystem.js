jest.unmock('../filesystem.jsx');

const path = require('../filesystem.jsx');

describe('Filesystem', () => {
  it('does a lot of cool stuff', () => {
  });
});

describe('File', () => {
  it('does a lot of cool stuff', () => {
  });
});

describe('Directory', () => {
  it('does a lot of cool stuff', () => {
  });
});