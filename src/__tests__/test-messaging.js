jest.unmock('../messaging.js');

const path = require('../messaging.js');

describe('broadcast', () => {
  it('does a lot of cool stuff', () => {
  });
});

describe('hasPort', () => {
  it('does a lot of cool stuff', () => {
  });
});

describe('getSource', () => {
  it('does a lot of cool stuff', () => {
  });
});

describe('define', () => {
  it('does a lot of cool stuff', () => {
  });
});

describe('process', () => {
  it('does a lot of cool stuff', () => {
  });
});